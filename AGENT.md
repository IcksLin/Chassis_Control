# Chassis_Control 开发约定

## 目录与职责

- `components/device/`：物理外设驱动，只负责总线、寄存器和设备协议。
- `components/algorithm/`：不依赖具体调度方式的算法与 HAL 数据映射。
- `components/system/`：服务编排、网络、网页和 FreeRTOS 任务。
- `main/`：只保留应用入口，不堆叠具体初始化逻辑。
- `components/algorithm/fusion/`：第三方 Fusion 库，除非确有必要，不直接修改。

新增功能应放入职责对应的文件。不得将外设初始化、Wi-Fi、Web、算法状态和任务创建重新合并到一个函数中。

## 统一初始化流水线

整个工程必须严格按照以下顺序启动：

1. 外设初始化（Peripheral）
2. 服务初始化（Service）
3. 中断或线程启动（Interrupt/Task）

顶层流水线由以下函数组成：

```c
chassis_runtime_init_peripherals();
chassis_runtime_init_services();
chassis_runtime_start_tasks();
```

`chassis_runtime_start()` 只能按上述顺序调用三个阶段，不得穿插执行。

### 第一阶段：外设

- 初始化 I2C、UART、传感器和执行器等物理设备。
- 校验设备地址、芯片 ID 和必要寄存器配置。
- 电机串口初始化后必须立即发送零速命令。
- 本阶段禁止创建业务线程或启用会周期读取设备的中断。
- 全部外设配置完成后必须等待至少 200 ms 的稳定时间，稳定期结束后才能设置“外设就绪”状态。

### 第二阶段：服务

- 初始化 HAL、互斥锁、事件组、姿态状态、Wi-Fi 和 HTTP 等服务。
- 服务函数必须检查依赖的外设阶段是否已经完成；禁止越级访问未就绪设备。
- 每个服务使用独立初始化函数，例如：

```c
imu_runtime_service_init();
wifi_service_init();
imu_web_service_init();
```

- 初始化函数应具有幂等保护；重复调用时返回 `ESP_OK`，不得重复创建资源。

### 第三阶段：中断或线程

- 只有外设和服务全部就绪后才能进入本阶段。
- 服务初始化完成后必须再等待至少 200 ms，才允许创建周期任务或启用业务中断。
- IMU 采样、姿态解算、电机转发、串口接收和控制台任务统一在本阶段启动。
- 任务入口不得承担硬件初始化工作，只能使用已经初始化完成的接口。

## 延时与硬件保护

- IMU963RA 第一次 I2C 访问前保留 200 ms 上电保护延时。
- 外设阶段末尾保留 200 ms 稳定屏障。
- 服务阶段结束到任务启动之间保留 200 ms 启动屏障。
- 延时必须使用 `vTaskDelay(pdMS_TO_TICKS(...))`，不得使用阻塞式忙等待。
- 修改延时时必须说明对应的硬件或时序依据，不能为了缩短启动时间随意删除保护。
- 软件复位而外设未断电时，应先执行总线恢复，再进行设备探测。

## 安全约定

- 底盘放在地面时，不得自动启动电机。
- 上电、复位、初始化失败和任务创建失败时，电机目标速度必须保持为零。
- 单轮测试只能由明确的人工命令触发，退出或异常路径必须调用停止接口。
- 所有非零运动命令必须经过唯一所有者锁、会话代次和限时租约；网页失联或心跳超时后不得由周期任务替旧命令续期。
- 电机下位机反馈超时、UART 写失败和控制源退出必须原子撤销运动意图，并由 50 Hz 转发任务持续发送零速覆盖，不能只依赖单个 STOP 包。
- 上下位机 UART 的一次完整协议帧必须在发送互斥锁内完成，禁止不同任务交叉写入形成拼接帧。
- 若需要覆盖 ESP 到下位机连线永久断开的故障，必须在下位机固件实现独立命令看门狗或增加硬件使能链路；上位机重复发零速无法跨越物理断线。
- 磁力计完成轴向确认和对北校准前，只允许采集和显示，不参与姿态融合。
- 不得在 ISR 中进行 I2C、HTTP、日志打印或复杂浮点姿态解算。

## 函数与错误处理

- 每个初始化函数只负责一个明确模块或阶段。
- 有顺序依赖的函数必须在入口检查状态，顺序错误返回 `ESP_ERR_INVALID_STATE`。
- 初始化失败应返回具体 `esp_err_t`，由上层使用 `ESP_RETURN_ON_ERROR` 附带阶段信息继续上报。
- 设备初始化中的瞬态总线错误应采用有限次数重试，禁止无限重启掩盖故障。
- 公开接口声明放在对应 `include/` 目录，内部辅助函数保持 `static`。

## 注释风格

注释参考 Abandoned 仓库项目代码，统一使用 Doxygen 风格中文说明。

每个自编写源文件和头文件应包含：

```c
/**
 * @file module_name.c
 * @brief 模块职责的简明说明
 */
```

函数定义使用以下格式，并只保留实际适用的字段：

```c
/**
 * @brief 执行函数的核心功能
 * @param input 输入参数含义与单位
 * @param output 输出参数含义与单位
 * @return esp_err_t ESP_OK 表示成功，其他值表示失败原因
 * @details 需要展开说明的处理流程
 * @note 调用顺序、线程安全或硬件安全要求
 */
```

具体要求：

- `@brief` 描述“做什么”，避免重复函数名。
- 所有参数都应使用 `@param` 说明方向、单位和有效范围。
- 非 `void` 函数必须使用 `@return` 说明返回值。
- 硬件时序、调用前置条件、线程上下文和安全限制使用 `@note`。
- 复杂初始化流程可使用 `@details`，但不要逐行翻译代码。
- 结构体、枚举和公开接口在头文件中提供简短说明。
- 第三方代码维持其原始注释与许可证，不批量改写。
- 修改实现时必须同步更新已失效的注释。

## 变更验证

- 修改 C/C++ 代码后至少执行一次 `idf.py build`。
- 涉及初始化顺序时，检查工程内全部 `xTaskCreate*`、定时器和中断注册位置，确认没有提前启动的业务任务。
- 烧录验证前确认电机速度为零；没有用户明确授权时，不运行电机测试。
- IMU 验证应分别检查主器件 ID、磁力计 ID、数据有效标志和连续数据变化。
